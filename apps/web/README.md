# Desktop App

This desktop app is the 3D environment for the portfolio website.

## Build

To build the desktop app, run the following command from the `app/desktop` directory:

```bash
npm run build
```